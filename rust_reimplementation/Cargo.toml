[package]
name = "cjson_rs"
version = "0.1.0"
edition = "2024"

[dependencies]
# 用于JSON序列化/反序列化
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

# 用于命令行参数解析（可选）
clap = { version = "4.0", features = ["derive"], optional = true }

# 用于更好的错误处理
anyhow = "1.0"
thiserror = "1.0"

